# Worklog 2026-05-22 (Handoff cho session sau)

> Mục tiêu phiên: "Demo dashboard cho CSKH/BS dùng thử SỚM" bằng data thật trích mẫu nhỏ.
> Kết phiên ở: tìm ra đúng nguyên nhân dashboard /patients trắng, CHƯA fix.

## Trạng thái commit
- Bắt đầu phiên: HEAD = 487b98f (origin/main, clean).
- Local ahead origin = 2 commit, CHƯA push:
  - 3e5a242  feat(seed): sample ETL 30 patients from admin CSV  (Task A)
  - e407ce6  feat(seed): bơm 30 BN demo qua patient_service + safe --wipe  (Task B)
- DB Supabase: 30 BN demo đang sống ở location "Kim Ngưu". patient_code = BN-2026-XXXXXX (service sinh).

## DONE phiên này
- Task A — scripts/seed/sample_from_csv.py: đọc CSV hành chính (1.130 dòng raw → 601 record pandas đọc đúng quote → 588 hợp lệ → 30 written). Dùng cột "//họ tên (neat)" + "//sdt (neat)" sạch sẵn (KHÔNG phải regex bóc tách). Xuất seed_sample.json (gitignored). ruff/mypy pass.
- Task B — scripts/seed/demo_seed.py: bơm 30 BN qua patient_service.create_patient (KHÔNG insert thẳng). Seed/wipe/re-seed counts khớp. --wipe an toàn (chỉ xóa BN khớp phone trong seed_sample.json). created_new=30, mpi_matched=0, error=0.

## Phát hiện kiến trúc (quan trọng cho P11)
- MPI dedup: service LUÔN insert mới, KHÔNG return record cũ. Nghi vấn trùng đẩy vào mpi_merge_queue (cột thật: patient_id_a / patient_id_b, mig 005) chờ người duyệt. → P11 import 198k dòng cần một MÀN HÌNH DUYỆT merge queue. Note để không quên.
- Cột mpi_merge_queue: patient_id_a / patient_id_b (KHÔNG phải primary_patient_id).
- Bảng patient KHÔNG có cột lưu external_code/legacy. Demo đã DROP KHACH-xxxxx + gender (DTO không có gender). Khi làm full ETL P11 mới cân nhắc thêm cột legacy_code + gender một thể.
- PatientCreateDTO KHÔNG validate phone E.164 → giữ 0xxx nội địa chạy ngon, không bị reject.
- patient_code do service tự sinh (caller không truyền).

## BUG ĐANG MỞ — ưu tiên #1 session sau
**Triệu chứng:** /patients trắng, hiển thị "Chưa có BN" dù DB có 30 BN.
**Bằng chứng (F12):**
- Console: KHÔNG có lỗi đỏ (chỉ warning preload vô hại của Next.js).
- Network: CHỈ 2 request, đều stylesheet 304. KHÔNG có request nào gọi Supabase REST (/rest/v1/patient).
- Env: .env.local CÓ ĐỦ NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_ANON_KEY.
- Đăng nhập OK (avalook.dev@gmail.com hiển thị).
**Chuẩn đoán (chưa verify bằng fix):** Trang fetch ở Server Component (SSR) nên client Network không thấy request. Fetch server trả rỗng. Nguyên nhân gần như chắc chắn = RLS policy trên bảng `patient` CHƯA mở cho role authenticated/anon đọc → Supabase trả [] im lặng. Data seed bằng service_role (bypass RLS) nhưng đọc bằng anon key (chịu RLS) → kinh điển.
**Việc cần làm session sau:**
1. Kiểm RLS: SELECT * FROM pg_policies WHERE tablename='patient'; — xem có policy SELECT cho authenticated chưa.
2. Nếu thiếu → thêm migration policy SELECT cho authenticated (CHỈ đọc, read-only đúng tinh thần dashboard P7a). LƯU Ý: thêm policy = đụng security/RLS = phải cẩn trọng, KHÔNG mở anon đọc PII bừa. national_id_number phải tiếp tục bị ẩn (UI đã ghi "CCCD KHÔNG hiển thị").
3. Verify lại /patients render đủ 30 BN bằng MẮT trước khi sang Task C.

## Quyết định chốt phiên này
- D-N(xx) sys.path band-aid: chọn FIX GỐC = poetry install editable 1 lần (bỏ sys.path.insert trong demo_seed.py). CHƯA thực thi — gộp vào task đầu session sau.
- Demo dùng data thật trích mẫu (KHÔNG full ETL P11 — P11 vẫn chờ Quang chốt 3 câu).
- Giữ phone 0 nội địa cho demo.

## Thứ tự việc session sau
1. (ưu tiên) Fix RLS /patients + verify mắt thấy 30 BN.
2. poetry install editable, gỡ sys.path band-aid khỏi demo_seed.py.
3. Task C — Docker Compose + Tailscale cho phòng khám truy cập (CHỈ làm sau khi /patients render OK).
4. Cân nhắc seed thêm work_session + staff_task để 2 trang còn lại (/work-sessions, /tasks) cũng có data demo (Realtime đã enable cho staff_task).

## Vẫn BLOCKED (ngoài tầm Tuyền)
- P11 ETL 198k dòng: chờ Quang chốt 3 câu.
- P12 Zalo/Pancake real: chờ OA approve token.

## Nguồn data khách (tham khảo cho P11 sau)
- Path máy Quang: /Users/quangdang/Documents/ClinicAI/Data khách gửi
- 16 CSV (Notion export, mỗi table có bản thường + bản _all giống hệt → chỉ đọc bản thường).
- 93.976 file .md = subpage con từng BN (1 BN nhiều subpage → cần gom trước dedup).
- File nhỏ nhất/sạch nhất = "File khách hàng (hành chính)" 1.130 dòng → đã dùng cho demo.
- File lâm sàng 164k dòng + Lịch hẹn 81k + CSKH Action 79k + Kê thuốc 30k + Dịch vụ 24k + Xét nghiệm 6k → để P11.
- Lưu ý: cell có newline nội bộ (Notion) → BẮT BUỘC dùng pandas/csv module, KHÔNG split tay.
